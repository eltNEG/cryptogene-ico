'use strict';

const React = require('react');

const ProjectDescription = React.createClass({

    render: function () {
        return (
            <section id='project-description'>
                <div className='row'/>
            </section>
        );
    }
});

module.exports = ProjectDescription;
